#include <stdio.h>
#include "max/max.h"
#include "min/min.h"

int main(int argc, char **argv) {
  printf("The max of integer 1 and 2 is: %d\n", max(1, 2));
  printf("The min of integer 1 and 2 is: %d\n", min(1, 2));
  return 0;
}
